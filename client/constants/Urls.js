// react-router URLs
//  no need to worry about endpointPrefix here, it's configured as the basename shared by all routes in index.jsx
export const homeUrl = '/';
export const loginUrl = '/login';
export const logoutUrl = '/logout';
